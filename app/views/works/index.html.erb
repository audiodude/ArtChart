<%= form_tag({:action => :search}, {:method => 'get'}) do %>
Search: <%= text_field_tag :q, params[:q], :size => 40 %> <%= submit_tag 'Go' %>
<% end %>
<table>
  <tr>
    <th>Name of work</th>
    <th>Artist</th>
    <th>Location</th>
  </tr>

  <%= render :partial => 'work', :collection => @works %>
</table>

<%= will_paginate @works %>
